import React from 'react';
import { Text, ScollView, ScrollView } from 'react-native';
import { Card, Button } from 'react-native-elements'

const Rufis = ({ navigation })=>{
    return(
        <ScrollView>
            
        </ScrollView>
    )
}

export default Rufis